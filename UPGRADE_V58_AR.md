# ترقية منصة مستر محمود فوزي — V58.0.0

## ما تم تنفيذه

- إضافة اختيار صورة QR من معرض الهاتف داخل بوابة الطالب وبوابة ولي الأمر.
- قبول QR يحتوي على كود مباشر أو رابط للبوابة، مع استمرار دعم الكاميرا.
- تطوير بوابتي الطالب وولي الأمر بصريًا وتحسين العرض على الموبايل.
- تطوير الامتحانات لتدعم الاختيار من متعدد والتصحيح التلقائي، والأسئلة المقالية والتصحيح اليدوي، وملف PDF، ومواعيد الفتح والإغلاق، وحفظ محاولة الطالب.
- تطوير PWA: زر تثبيت عام، حالة الاتصال، فتح النسخة المثبتة في النافذة القائمة، وتحسين التعامل مع الإشعارات.
- تحديث واجهة الإدارة والأزرار والقوائم والكروت مع الحفاظ على هوية منصة مستر محمود فوزي الزرقاء.

## منع تسجيل الطالب أكثر من مرة

- الاسم يجب أن يكون ثلاثيًا على الأقل في الحجز العام، والإضافة من الإدارة، والاستيراد.
- إزالة التشكيل والتطويل وعلامات الترقيم، وتوحيد المسافات وأشكال الألف والياء قبل المقارنة.
- إنشاء هوية مشفرة ثابتة للاسم داخل `_student_name_claims`.
- حجز الاسم والطالب في عملية ذرية واحدة، لذلك لا يمكن نجاح طلبين متزامنين لنفس الاسم.
- فحص سجلات الطلاب والحجوزات القديمة التي أُنشئت قبل النظام الجديد.
- عند وجود تطابق، تظهر رسالة تطلب الاسم الرباعي لتمييز الطالب.
- عند رفض حجز أو حذف طالب حذفًا آمنًا، يتم تحرير حجز الاسم.

## التحقق

- `npm test`: ناجح.
- فحص JavaScript وJSON وروابط HTML والمعرّفات: ناجح.
- فحوص Cloud Functions وFirestore Rules والامتحانات والـQR والـPWA: ناجحة.
- `npm run build`: ناجح، وتم إنشاء `dist` للإنتاج.

## النشر

نفّذ من مجلد المشروع:

```powershell
npm install
npm --prefix functions install
npm test
npm run build
firebase login
firebase use mahmoud-fawzy-science-platform
firebase deploy --only firestore:rules,firestore:indexes,storage,functions,hosting
```

إذا كان اسم مشروع Firebase مختلفًا، استبدل `mahmoud-fawzy-science-platform` بالمعرّف الصحيح الظاهر في Firebase Console.
