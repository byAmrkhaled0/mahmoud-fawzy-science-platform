# تقرير الفحص والإصلاح — منصة مستر محمود فوزي V59.4.6

## النتيجة المختصرة

تم العثور على سبب اختلاف حالة الامتحان بين لوحة الإدارة وصفحة الطالب وإصلاحه في الواجهة ووظائف Firebase. النسخة المنشورة التي فُحصت كانت ما تزال تعرض أصول الإصدار `59.4.5`، بينما الإصلاح موجود في هذه الحزمة بالإصدار `59.4.6` ويحتاج نشر وظائف Firebase ثم واجهة الموقع.

## السبب الأساسي للمشكلة

كان هناك مساران لإنشاء الامتحان:

- المسار الموحد يحول موعد الفتح والإغلاق إلى ISO كامل.
- المسار القديم يحفظ قيمة `datetime-local` من غير منطقة زمنية، مثل `2026-07-31T18:00`.

لوحة الإدارة كانت تقرأ هذه القيمة كتوقيت محلي في مصر، بينما Cloud Functions تقرأها كتوقيت UTC. في التوقيت الصيفي ينتج فرق ثلاث ساعات، ولذلك قد تعرض لوحة الإدارة «انتهى» بينما يظل زر «بدء الامتحان» ظاهرًا للطالب.

## الإصلاحات المنفذة

1. توحيد قراءة وحفظ المواعيد على توقيت `Africa/Cairo` صيفًا وشتاءً.
2. دعم الامتحانات القديمة المحفوظة من غير منطقة زمنية من غير الحاجة إلى تعديل يدوي للبيانات.
3. إرجاع الامتحانات المنتهية في لوحة الطالب بدل حذفها من استجابة الخادم.
4. عرض ثلاث حالات صريحة:
   - قبل الموعد: «ترقّب الامتحان وذاكر ببراعة» مع عداد الفتح.
   - أثناء الموعد: «الامتحان متاح الآن» مع الوقت المتبقي.
   - بعد الإغلاق: «انتهى الوقت — لم تستطع أداء الامتحان هذه المرة» وزر معطل.
5. مزامنة عداد الطالب مع ساعة الخادم بدل الاعتماد الكامل على ساعة جهازه.
6. إصلاح انتقال العداد من «قادم» إلى «متاح» ثم إلى «منتهٍ» من غير إعادة تحميل.
7. منع الخادم من بدء الامتحان قبل الفتح أو بعد الإغلاق حتى لو تم استدعاء الدالة مباشرة.
8. جعل نهاية جلسة الامتحان هي الأقرب بين مدة المحاولة وموعد الإغلاق العام.
9. إغلاق ثغرة الإرسال التلقائي التي كانت تسمح بتجاوز مهلة انتهاء الجلسة.
10. توحيد عرض المواعيد والحالة في لوحة الإدارة على توقيت القاهرة.

## فحص المنصة

- الصفحة المنشورة للامتحانات فتحت بنجاح، وروابط الواجهة والأصول الحرجة كانت من الإصدار `59.4.5` وقت الفحص.
- فحص صحة JavaScript وJSON: ناجح.
- فحص معرفات HTML والمراجع المحلية: ناجح.
- فحص 80 معالج إجراء مباشر في الواجهة: ناجح.
- فحص ربط خدمات الواجهة بوظائف Firebase: ناجح.
- فحص قواعد Firestore ومنع الكتابات العامة الحساسة: ناجح.
- فحص PWA والكاش وأصول النشر الحرجة: ناجح.
- فحص منطق الامتحانات الجديد وتوقيت القاهرة: 27 اختبارًا ناجحًا، صفر فشل.

## حدود الفحص المباشر

تم فحص الصفحات العامة والرابط المنشور. عرض بيانات امتحان طالب فعلي يحتاج كود طالب صالح، ولوحة الإدارة تحتاج حساب فريق عمل؛ لم تُستخدم أو تُخمن أي بيانات دخول. عُوّض ذلك باختبارات آلية للحالات الثلاث وحدود الوقت الدقيقة، وبفحص منطق الخادم والواجهة كاملًا.

## ترتيب النشر المطلوب

يجب نشر وظائف Firebase أولًا، ثم بناء ونشر الواجهة. نشر واجهة Vercel وحدها لا يكفي لأن جزءًا أساسيًا من الإصلاح موجود في `functions/index.js` و`functions/lib/exam-schedule.js`.

