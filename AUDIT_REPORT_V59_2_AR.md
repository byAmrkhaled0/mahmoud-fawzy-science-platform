# تقرير فحص منصة مستر محمود — V59.4.5

## النتيجة

النسخة سنتر فقط، ومربوطة بمشروع Firebase الخاص بمستر محمود. لم تتم إضافة صفحة أونلاين أو محاضرات أونلاين أو اشتراكات أونلاين أو وظائف Firebase خاصة بالأونلاين.

## طلب النقل

- بوابة الطالب تقرأ المجموعات مباشرة من مجموعة `groups` في Firestore.
- لا تظهر إلا المجموعات النشطة المطابقة لصف الطالب.
- تتم مطابقة الترم والعام الدراسي عند تسجيلهما على الطالب والمجموعة.
- لا تظهر مجموعة الطالب الحالية.
- عند تحديد سعة قصوى للمجموعة، لا تظهر المجموعة المكتملة للطالب.
- يظهر اسم المجموعة والأيام ووقت البداية والنهاية وعدد الأماكن المتبقية إن وُجد.
- لا يستطيع الطالب إرسال أكثر من طلب معلق.
- موافقة المدرس تحدّث المجموعة والميعاد في `students` و`student_portal` و`parent_portal` و`payments`.
- يعاد فحص نشاط المجموعة والصف والترم والسعة وقت الموافقة؛ لذلك لا يتم نقل الطالب إلى موعد أصبح غير صالح.

## لوحة الإدارة

- نفس أسلوب لوحة مستر سعد: شريط جانبي داكن، أيقونات واضحة، أقسام مجمعة، ملخص مهام واختصارات.
- بحث شامل عن الطالب أو الحجز أو طلب النقل.
- إضافة سريعة: طالب، مجموعة وموعد، واجب، امتحان، ومسح حضور QR.
- ترتيب التشغيل اليومي: الرئيسية، الحجوزات، الطلاب، المجموعات والمواعيد، الحضور، تحذيرات الغياب، طلبات الطلاب، واشتراك السنتر.
- أقسام التعليم: الواجبات، الامتحانات، والمراجعات والأسئلة.
- لم تُنقل أي أزرار أو مسارات أونلاين من منصة مستر سعد.

## الصفوف والمجموعات والفلاتر

- فلاتر تحذيرات الغياب وطلبات الطلاب والحجوزات تستمد الصفوف والمجموعات من بيانات Firebase والطلاب المرتبطين.
- تغيير الصف يعيد بناء قائمة المجموعات المطابقة فقط.
- المطابقة تتحمل اختلاف الأرقام العربية والإنجليزية، المسافات، التشكيل، وأشكال الألف.
- إضافة الطالب تعرض مواعيد الصف المختار فقط.
- إدارة المجموعة تسمح بتحديد الصف والترم والعام الدراسي والأيام والوقت والسعة.

## الهاتف

- قائمة إدارة جانبية قابلة للفتح والإغلاق.
- شريط تنقل سفلي ثابت بخمسة اختصارات.
- زر إضافة سريعة فوق شريط الهاتف.
- الفلاتر والنماذج تتحول إلى عمود واحد على الشاشات الصغيرة.
- بطاقات تحذير الغياب وطلبات النقل والحجوزات لها أزرار لمس واضحة.
- تبويبات بوابة الطالب قابلة للسحب أفقيًا، ومنها تبويب طلب النقل.
- منع التمدد الأفقي وتحسين الجداول والحوارات والحقول على الهاتف.

## نتائج الاختبارات

- فحص JavaScript وJSON: ناجح.
- فحص معرفات HTML والروابط المحلية: ناجح.
- فحص 79 إجراء زر داخل HTML: ناجح.
- اختبارات منطق الصفوف والمجموعات والواجبات والأسماء وطلب النقل: 16 من 16 ناجحة.
- بناء نسخة `dist`: ناجح بإصدار `59.4.5`.
- إصلاح تمرير معاملات أوامر `npm` وFirebase وGit داخل PowerShell على Windows.
- تحميل Firebase Functions محليًا بعد تثبيت الاعتماديات: 128ms.
- تم التعرف على 25 وظيفة، ومنها `getPortalStudent` و`createStudentTransferRequest` و`reviewStudentTransferRequest`.

## النشر

من داخل مجلد المشروع على PowerShell:

```powershell
Set-ExecutionPolicy -Scope Process Bypass -Force
npm run deploy:production
```

السكريبت يشغّل الاختبارات والبناء، يثبت اعتماديات Functions، يرفع Firebase Functions والقواعد والفهارس، ثم يرفع التعديلات إلى GitHub إذا كان المجلد مربوطًا بالريبو.
